import 'package:flutter/material.dart';

class RahimApp extends StatelessWidget {
  const RahimApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Kere Mama Ke Kovor"),
      ),
    );
  }
}
